# This is an example .env file. Copy this to .env and fill in your actual values.

# OpenRouter API Key (Required)
# Get your API key from https://openrouter.ai/keys
OPENROUTER_API_KEY=""

# Maximum budget for a benchmark run in USD (Optional, Default: 100.0 in config.py)
# The benchmark will attempt to stop if this budget is exceeded.
# MAX_BUDGET_USD=50.0

# Default Judge LLM (Optional, Default: "openai/gpt-4o" in config.py)
# It is recommended to use a powerful model for judging.
# Examples: "anthropic/claude-3-opus-20240229", "google/gemini-1.5-pro-latest"
# JUDGE_MODEL_NAME="anthropic/claude-3-opus-20240229"

# Default Log Level for the application (Optional, Default: "INFO" if not set otherwise)
# Supported levels: DEBUG, INFO, WARNING, ERROR, CRITICAL
# This can be overridden by the --log-level CLI option.
# LOG_LEVEL="DEBUG"
